[package]
name = "prompter"
version = "0.1.0"
edition = "2021"

[dependencies]
zbus = { version = "5.3.1", features = ["tokio"], default-features = false }
tokio = { version = "1.44", features = ["full"] }
